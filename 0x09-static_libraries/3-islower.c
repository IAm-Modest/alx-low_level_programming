#include "main.h"

/**
  * _islower - ckecks for lower case alphabets
  * @c: the letter to check
  * Return: 1 if @c is lowercase and 0 if not
  */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

