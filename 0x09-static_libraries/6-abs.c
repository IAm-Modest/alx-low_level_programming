#include "main.h"

/**
  * _abs - produces the absolute value of an integer
  * @n: integer to check
  * Return: the absolute value of integer
  */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
