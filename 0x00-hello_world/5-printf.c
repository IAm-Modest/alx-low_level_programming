#include <stdio.h>
/**
  * main - starts the program
  * printf: prints out its content
  * Return: 0 if it runs successfully
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
