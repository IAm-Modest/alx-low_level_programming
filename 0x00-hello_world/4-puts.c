#include <stdio.h>
/**
 * main -  marks the beginning of the programme
 * Return: 0 if the ptogramme runs successfully
 * puts: prints its content to the stdout
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}}
